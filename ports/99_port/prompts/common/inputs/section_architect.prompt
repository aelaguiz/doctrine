import .runtime
import .continuity
import ..surfaces.section


# Consumer bundle for `SectionArchitect`:
# - runtime.CurrentIssuePlanAndRoute
# - section.SectionDossierContract
# - section.SectionConceptsAndTermsContract
# - continuity.SectionSizingContinuityContext
#
# This lane consumes the locked section contracts plus conditional continuity
# evidence when lesson-count sizing still depends on nearby sections.
