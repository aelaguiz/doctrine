import .runtime
import .checkout
import ..surfaces.lesson
import ..surfaces.support


# Consumer bundle for `PlayableMaterializer`:
# - runtime.CurrentIssuePlanAndRoute
# - checkout.AttachedCheckoutRoot
# - lesson.LessonPlanContract
# - lesson.LessonSituationsContract
# - support.ActionAuthorityProof
#
# This lane consumes the locked lesson plan and situation set, the attached
# checkout for live route and validation truth, and exact-move proof only when
# it still applies.
