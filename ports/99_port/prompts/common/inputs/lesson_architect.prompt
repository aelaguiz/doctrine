import .runtime
import .continuity
import ..surfaces.section


# Consumer bundle for `LessonArchitect`:
# - runtime.CurrentIssuePlanAndRoute
# - section.SectionLessonMapContract
# - section.SectionConceptsAndTermsContract
# - section.SectionPlayableStrategyContract
# - continuity.NearbyLessonContinuityContext
#
# This lane consumes the locked section contracts plus nearby lesson continuity
# only when pacing still depends on it.
