import .runtime
import ..surfaces.section
import ..surfaces.lesson
import ..surfaces.support


# Consumer bundle for `AcceptanceCritic`:
# - runtime.CurrentIssuePlanAndRoute
# - runtime.CurrentHandoffComment
# - runtime.CurrentPacketInScope
# - runtime.UpstreamPacketChain
# - packet-type-specific semantic surfaces when the reviewed packet depends on
#   them
#
# This lane consumes review scope and upstream truth. It does not consume its
# own review artifact as a required input.
