import .runtime
import ..surfaces.section
import ..surfaces.lesson
import ..surfaces.review


# Consumer bundle for `ProjectLead`:
# - runtime.ActiveIssue
# - runtime.CurrentIssuePlanAndRoute
# - runtime.CurrentHandoffComment
# - runtime.CurrentPacketInScope
# - runtime.UpstreamPacketChain
# - runtime.CurrentPublishState
#
# This lane consumes route state, packet-chain truth, and publish state when it
# is live. It does not consume its own coordination artifact as a required
# input.
