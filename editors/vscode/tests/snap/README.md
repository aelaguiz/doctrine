# Snapshot Fixtures

This tree mirrors the repo-relative path layout under `examples/**/prompts/**`.

Use the same relative path under `tests/snap/examples/` to find the source prompt
file that a snapshot fixture came from.
