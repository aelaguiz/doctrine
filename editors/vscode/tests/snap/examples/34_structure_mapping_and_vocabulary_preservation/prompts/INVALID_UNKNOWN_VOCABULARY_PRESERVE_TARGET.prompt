# This file is intentionally invalid.
# The workflow tries to preserve vocabulary from an unknown target.

# The declarations below are just enough setup to isolate the one broken rule this file is testing.
input ReviewTemplate: "Review Template"
    source: File
        path: "unit_root/_authoring/review_template.md"
    shape: MarkdownDocument
    requirement: Required


# The declarations below are just enough setup to isolate the one broken rule this file is testing.
output CoordinationHandoff: "Coordination Handoff"
    target: TurnResponse
    shape: Comment
    requirement: Required

    # This field carries the name of whatever artifact is current right now.
    current_artifact: "Current Artifact"
        "Name the one artifact that is current now."

    # Only the fields listed here are trustworthy readback for the next owner.
    trust_surface:
        current_artifact


# The declarations below are just enough setup to isolate the one broken rule this file is testing.
workflow InvalidPreserveVocabulary: "Preserve Vocabulary"
    # This block holds the machine-checked rules for the turn.
    law:
        # This line says what artifact is current now and which output field must carry that fact forward.
        current artifact ReviewTemplate via CoordinationHandoff.current_artifact
        # This line says the closed term set must stay stable.
        # This vocabulary target does not resolve to a known enum or declaration.
        preserve vocabulary MissingVerdictEnum


# The declarations below are just enough setup to isolate the one broken rule this file is testing.
agent InvalidUnknownVocabularyPreserveTarget:
    role: "Trigger an unknown vocabulary preserve target."
    workflow: InvalidPreserveVocabulary
    outputs: "Outputs"
        CoordinationHandoff
