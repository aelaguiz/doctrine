interface:
  display_name: "Doctrine Learn"
  short_description: "Teach an agent how to author Doctrine end-to-end: principles, language, packaging, emit, verify"
  default_prompt: "Use $doctrine-learn to teach me how to author <the surface I'm about to write>, then guide me through a minimal working example."

policy:
  allow_implicit_invocation: true

dependencies:
  tools: []
