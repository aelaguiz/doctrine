# Candidate Tests

## Source Summary

One short paragraph on what this source actually does.

## High-Signal Patterns

- Pattern:
  Concrete commands early in the file.
  Doctrine pull:
  Example or doc guidance that keeps commands early and explicit.

- Pattern:
  Nested instruction files override a root instruction file.
  Doctrine pull:
  Inheritance or scope-precedence example pressure.

## Candidate Doctrine Examples

- Title:
  Short candidate example title.
  Main bucket:
  `scope_hierarchy`
  Likely surface:
  workflow inheritance, scoped docs, or AGENTS precedence guidance.
  Why this stays generic:
  Replace upstream product names with generic declarations.

## Candidate Diagnostics

- Diagnostic idea:
  Short compile-fail or parse-fail idea.
  Failure to prove:
  What should fail loud.

## Keep Out

- Product names or company-specific jargon that should not enter public Doctrine
  examples.
- Upstream repo details that are only local workflow habits, not reusable
  language pressure.
