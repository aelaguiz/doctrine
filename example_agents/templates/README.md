# Templates

Use these files when creating a new harvested source package under
`example_agents/harvested/`.

- `SOURCE.toml`: metadata contract for one source package
- `CANDIDATE_TESTS.md`: Doctrine-facing extraction template

The goal is to keep every harvested package consistent enough that we can search
and compare them mechanically later.
