# Example Placeholder: Merge Semantics

This placeholder does not contain the actual example yet.

This example is meant to show how inherited fields merge when both parent and child define the same surface.

It should cover deterministic behaviors like `append`, `override`, and `replace`.
