schema_version = 1
default_prompt = "prompts/AGENTS.prompt"

[[cases]]
name = "runtime agent packages build the canonical runtime tree"
status = "active"
kind = "build_contract"
build_target = "example_115_runtime_agent_packages"
