schema_version = 1
default_prompt = "prompts/SKILL.prompt"

[[cases]]
name = "runtime metadata files emit beside the main skill package"
status = "active"
kind = "build_contract"
build_target = "example_98_skill_package_runtime_metadata"
