# This file is intentionally invalid.
# The workflow claims ownership of a field and also says to preserve that same field exactly.

# The declarations below are just enough setup to isolate the one broken rule this file is testing.
output SectionMetadata: "Section Metadata"
    target: File
        path: "unit_root/_authoring/section_metadata.json"
    shape: JsonObject
    requirement: Required


# The declarations below are just enough setup to isolate the one broken rule this file is testing.
output CoordinationHandoff: "Coordination Handoff"
    target: TurnResponse
    shape: Comment
    requirement: Required

    # This field carries the name of whatever artifact is current right now.
    current_artifact: "Current Artifact"
        "Name the one artifact that is current now."

    # Only the fields listed here are trustworthy readback for the next owner.
    trust_surface:
        current_artifact


# The declarations below are just enough setup to isolate the one broken rule this file is testing.
workflow InvalidNarrowMetadataEdit: "Narrow Metadata Edit"
    # This block holds the machine-checked rules for the turn.
    law:
        # This line says what artifact is current now and which output field must carry that fact forward.
        current artifact SectionMetadata via CoordinationHandoff.current_artifact
        # This line limits the edit authority to exactly the named fields.
        own only SectionMetadata.name
        # This line says everything outside the carve-out must stay exactly as it was.
        # This exact-preservation rule still includes `SectionMetadata.name`, which the workflow already said it owns.
        preserve exact SectionMetadata.*


# The declarations below are just enough setup to isolate the one broken rule this file is testing.
agent InvalidOwnAndExactPreserveOverlap:
    role: "Trigger owned scope that overlaps exact preservation."
    workflow: InvalidNarrowMetadataEdit
    outputs: "Outputs"
        SectionMetadata
        CoordinationHandoff
