schema_version = 1
default_prompt = "prompts/AGENTS.prompt"

[[cases]]
name = "skill graph can document an allowed cross-flow cycle and branch coverage warning"
status = "active"
kind = "build_contract"
build_target = "example_161_skill_graph_policy_allowances"
