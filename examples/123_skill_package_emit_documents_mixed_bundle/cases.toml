schema_version = 1
default_prompt = "prompts/SKILL.prompt"

[[cases]]
name = "skill package emit block can mix emitted docs with bundled agents and raw files"
status = "active"
kind = "build_contract"
build_target = "example_123_skill_package_emit_documents_mixed_bundle"
