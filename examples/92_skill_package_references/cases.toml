schema_version = 1
default_prompt = "prompts/SKILL.prompt"

[[cases]]
name = "reference companion emits under references with authored markdown"
status = "active"
kind = "build_contract"
build_target = "example_92_skill_package_references"
