# Example Placeholder: Writer

This placeholder does not contain the actual example yet.

This output is meant to show one emitted agent document in a multi-agent example.

The important part here is the directory shape:
- one folder per agent name
- one `AGENTS.md` per agent output
- multiple agent outputs side by side under `ref/`
