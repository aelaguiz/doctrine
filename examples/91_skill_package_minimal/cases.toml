schema_version = 1
default_prompt = "prompts/SKILL.prompt"

[[cases]]
name = "minimal skill package builds the canonical skill tree"
status = "active"
kind = "build_contract"
build_target = "example_91_skill_package_minimal"

[[cases]]
name = "skill package metadata rejects unknown fields"
status = "active"
kind = "parse_fail"
prompt = "prompts/INVALID_UNKNOWN_METADATA.prompt"
exception_type = "ParseError"
error_code = "E199"
message_contains = [
  "Unknown skill package metadata field",
  "nonsense",
]
