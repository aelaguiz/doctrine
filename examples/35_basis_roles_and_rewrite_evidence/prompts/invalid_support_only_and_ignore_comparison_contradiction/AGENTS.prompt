# This file is intentionally invalid.
# The same input is declared comparison-only support and then ignored for comparison.

# The declarations below are just enough setup to isolate the one broken rule this file is testing.
input AcceptedPeerSet: "Accepted Peer Set"
    source: File
        path: "catalog/accepted_peers.json"
    shape: JsonObject
    requirement: Advisory


# The declarations below are just enough setup to isolate the one broken rule this file is testing.
output SectionMetadata: "Section Metadata"
    target: File
        path: "unit_root/_authoring/section_metadata.json"
    shape: JsonObject
    requirement: Required


# The declarations below are just enough setup to isolate the one broken rule this file is testing.
output CoordinationHandoff: "Coordination Handoff"
    target: TurnResponse
    shape: Comment
    requirement: Required

    # This field carries the name of whatever artifact is current right now.
    current_artifact: "Current Artifact"
        "Name the one artifact that is current now."

    # Only the fields listed here are trustworthy readback for the next owner.
    trust_surface:
        current_artifact


# The declarations below are just enough setup to isolate the one broken rule this file is testing.
workflow InvalidRewriteAwarePolish: "Rewrite-Aware Polish"
    # This block holds the machine-checked rules for the turn.
    law:
        # This line says what artifact is current now and which output field must carry that fact forward.
        current artifact SectionMetadata via CoordinationHandoff.current_artifact
        # This line allows the input to help with comparison without letting it become current truth.
        support_only AcceptedPeerSet for comparison
        # This line says the named input should not be used even as comparison help.
        # This line contradicts the earlier `support_only ... for comparison` rule for the same input.
        ignore AcceptedPeerSet for comparison


# The declarations below are just enough setup to isolate the one broken rule this file is testing.
agent InvalidSupportOnlyAndIgnoreComparisonContradiction:
    role: "Trigger contradictory comparison basis roles."
    workflow: InvalidRewriteAwarePolish
    outputs: "Outputs"
        SectionMetadata
        CoordinationHandoff
