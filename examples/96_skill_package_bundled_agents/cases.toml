schema_version = 1
default_prompt = "prompts/SKILL.prompt"

[[cases]]
name = "bundled agent prompts emit markdown companions and keep runtime metadata"
status = "active"
kind = "build_contract"
build_target = "example_96_skill_package_bundled_agents"
