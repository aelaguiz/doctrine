schema_version = 1
default_prompt = "prompts/SKILL.prompt"

[[cases]]
name = "plugin metadata files emit with preserved relative paths"
status = "active"
kind = "build_contract"
build_target = "example_99_skill_package_plugin_metadata"
