schema_version = 1
default_prompt = "prompts/SKILL.prompt"

[[cases]]
name = "skill package emits source receipt"
status = "active"
kind = "build_contract"
build_target = "example_147_skill_package_source_receipt"

