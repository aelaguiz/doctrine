schema_version = 1
default_prompt = "prompts/AGENTS.prompt"

[[cases]]
name = "flow visualizer showcase build proof stays checked in"
status = "active"
kind = "build_contract"
build_target = "example_73_flow_visualizer_showcase"
