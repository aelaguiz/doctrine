schema_version = 1
default_prompt = "prompts/AGENTS.prompt"

[[cases]]
name = "skill graph emits audit authoring metadata and graph-path repeat targets"
status = "active"
kind = "build_contract"
build_target = "example_163_skill_graph_authoring_metadata"
